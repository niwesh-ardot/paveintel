import { defaultSettings } from '@/lib/data/mockData';
export default function Settings(){return <div className='bg-white border rounded-xl p-6 space-y-3'><h2 className='text-2xl font-semibold'>Settings</h2><p className='text-sm'>AI provider: {defaultSettings.aiProvider}</p><p className='text-sm'>OpenAI API key: {process.env.OPENAI_API_KEY ? 'Configured' : 'Not configured (placeholder)'}</p><p className='text-sm'>Model: {defaultSettings.model}</p><p className='text-sm'>Embedding model: {defaultSettings.embeddingModel}</p><p className='text-sm'>Vector DB: {defaultSettings.vectorDb}</p><p className='text-sm'>Citation strictness: {defaultSettings.citationStrictness}</p></div>}
