import { documents, researchCards } from '@/lib/data/mockData';
export default function RC(){const rc=researchCards[0]; const doc=documents.find(d=>d.id===rc.documentId)!; return <div className='bg-white border rounded-xl p-6 space-y-2'><h2 className='text-2xl font-semibold'>Research Cards</h2><h3 className='font-semibold'>{doc.title}</h3><p className='text-sm'>Objective: {rc.objective}</p><p className='text-sm'>Study type: {rc.studyType} | Pavement: {rc.pavementType}</p><p className='text-sm'>Input variables: {rc.inputVariables.join(', ')}</p><p className='text-sm'>Key equations: {rc.equations.join('; ')}</p><p className='text-sm'>FWD details: {Object.entries(rc.fwdDetails??{}).map(([k,v])=>`${k}: ${v}`).join(' | ')}</p></div>}
